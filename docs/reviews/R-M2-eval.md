# Ревью M2 (evaluator, внешний, «докажи мне») — orc

Майлстоун M2: F5 admission, F6 бюджет, F7 watchdog, F8 восстановление, F9 гейт, F15 Ghostty-закрытие.
Раунд 1. Все прогоны — мои, руками. Живой claude НЕ жёгся. Все husk от моих прогонов убраны (финальный аудит: 0 husk, Terminal 15=baseline).

## Метод и сырые прогоны

### Смоук M1 (регресс?)
- `python3 -m pytest tests/ -q` → **175 passed** (заявлено 175 — совпало; перепрогнал дважды, стабильно). Продукт не мутирован моими прогонами.
- `bash .verify/e2e-loop-close.sh` (петля M1) → **ЗАВИС/ТАЙМАУТ 2 мин, EXIT 143**. Terminal-окна 15→15 (окна не создались). Причина: скрипт не форсит бэкенд → дефолт `terminal: ghostty` → Ghostty-спавн не исполняет seam-команду → STATE.md не пишется → диспетчер поллит вечно. **РЕГРЕССИЯ M1**: F14-петля была verified на Terminal, F15 переключил дефолт на нерабочий Ghostty → петля разомкнулась на дефолтном пути.

### F5 admission (G4/G12)
- `bash .verify/admission.sh` → PASS, exit 0. 6/6 фикстур классифицированы, гейт корректен на RAM/window/limit.
- Лимит-строки в `tests/fixtures/limit-*.txt` сверены с офиц. `code.claude.com/docs/en/errors` (WebFetch) — **дословно реальные** (session/weekly/Opus/429/529), не выдуманы.
- Мой fuzz (смешанные 429+session, регистр, битое время ресета, пусто): severe usage-cap побеждает транзиент; регистр не важен; битое `25:99`/`resets` → reset_epoch=None (без крэша). F5 устойчив.
- `pytest tests/test_admission.py` → 23 passed.
**F5: PASS.**

### F6 бюджет (G8)
- `bash .verify/budget.sh` → PASS, exit 0. Газета сводкой ПЕРВОЙ строкой (`смена: N готово...`), DONE/DONE-WAVE-N/BETA различимы, per-task расход показан, cap→парковка+стоп.
- Реальный ccusage проверен мной: total 42.9M, окно 246 мин, RAM 1819MB — probes живые.
- ОГОВОРКА: п.1 verify мерит арифметику `after−before` с ХАРДКОДОМ `tokens_now=before+12345`, НЕ реальную дельту от работы claude. Реальная дельта отложена на F12 (честно; окно берегут). «Расход = дельта ccusage реальна» доказано наполовину: арифметика + монотонность реального ccusage.
- `pytest tests/test_budget.py` → 15 passed.
**F6: PASS-с-оговоркой** (реальная атрибуция расхода не измерена до F12).

### F7 watchdog (G5) — 0 ложных kill критично
- `bash .verify/watchdog.sh` → PASS, exit 0. loop/silence детектятся; 200с in-flight → verdict ok (0 false kill); restart bounded, cap→escalate.
- **Мой независимый real-clock тест** (не инъекция now, живой child sleep 200 + маркер, порог silence 3с, реальные 5с ожидания): маркер РЕАЛЬНО держит воркер (busy=True → ok, 0 false kill); после снятия маркера + тишины>3с → silence; stale-маркер 1000с > staleness-границы → busy=False (мёртвый-в-tool отсекается). Маркер читается из файла с реальным временем — НЕ хардкод.
- `pytest tests/test_watchdog.py` → 18 passed.
**F7: PASS. 0 ложных kill подтверждено независимо.**

### F8 восстановление (G6) — реальный PID
- `bash .verify/kill-restart.sh` → PASS, exit 0. PID 6584 реальный, live, kill-0 работает — баг pid-None eval M1 закрыт БУКВАЛЬНО. ADOPT (живой воркер) + LEASE (kill-9 → задача пережила) проходят.
- **НО две находки:**
  1. **[БЛОКЕР] kill-restart.sh при рестарте-после-kill (стр.96) заспавнил РЕАЛЬНЫЙ claude** (`exec /opt/homebrew/bin/claude`, pid 6777/6782) — `ORC_SPAWN_CMD_OVERRIDE` передан только в первый `start`, не в рестарты. Это (а) жжёт окно ccusage вопреки заданию «беречь», (б) оставил husk, который cleanup скрипта НЕ убрал (cleanup написан под Terminal osascript, спавн — Ghostty). Я убрал husk руками.
  2. **[СУЩ] PID в Ghostty-режиме = PID Ghostty-обёртки, а не воркера** (проверил: `ps` показывает `Ghostty.app/.../ghostty -e ...`). Формально «не None», семантически — PID терминала-хоста. Так как воркер-команда не исполняется (см. F15), реального воркер-процесса нет.
- `pytest tests/test_recovery.py` → 11 passed.
**F8: DОРАБОТАТЬ** (реальный PID закрыт буквально, но seam-скрипт жжёт claude + оставляет husk; PID-семантика в Ghostty некорректна).

### F9 гейт (G2) — золотой путь
- `bash .verify/e2e-gate.sh` → **FAIL, exit 1, 3 провала** (builder заявил exit 0 self-pass — НЕ ВОСПРОИЗВОДИТСЯ у меня):
  - NOTIFY-CONTENT FAIL: диспетчер не запарковал задачу на гейте (осталась `in_progress`), уведомление не составлено.
  - CARD FAIL: газета показала «▸ в работе», а не gate-карточку (нет scope/brief/cost).
  - RESUME FAIL: STATE.md пуст.
- Диагностика: seam-воркер ДОЛЖЕН был написать `docs/tasks/<slug>/STATE.md` со `parked-on-gate`, но **директория не создана — команда не исполнилась** (Ghostty-спавн, см. F15). Диспетчер не увидел гейт → петля гейта разомкнута (тот же класс дефекта, что убил чекпойнт M1).
- Реальное osascript-уведомление отдельно: rc=0 (доставка работает).
- Логика gate-карточки в изоляции (я вызвал report.newspaper с parked-состоянием напрямую): карточка КОРРЕКТНА — скоуп/планка/полномочия/цена ошибки/путь ТЗ/маркер «необратимое — не в батче», plain-text, ⏸. То есть карточка готова, но гейт до неё не доходит из-за поломки спавна.
- `pytest tests/test_gate.py` → 8 passed (юниты мокают спавн, потому зелёные).
**F9: ДОРАБОТАТЬ (БЛОКЕР).** E2E-гейт не работает на реальном спавне.

### F15 Ghostty-закрытие — боль пользователя, фикс-фича
- `bash .verify/ghostty-close.sh` → PASS, exit 0. Спавн 3 pid под маркером, close_ghostty killed=3, window_closed=true, 0 husk после.
- Мой visual-proxy тест (`ghostty -e` process count): 1→2 при спавне → 1 после close, 0 маркер-процессов = окно реально закрылось.
- **НО КОРНЕВОЙ БЛОКЕР**: Ghostty-спавн НЕ ИСПОЛНЯЕТ команду воркера. Доказано многократно, детерминированно:
  - `build_inner_command` даёт `export ORC_SESSION=x; cd ... && <cmd>` как ОДНУ строку; `spawn_ghostty` строит `open -na Ghostty.app --args -e bash -lc <inner>`.
  - При запуске: процесс Ghostty жив, но `ran.txt`/STATE.md НИКОГДА не пишутся (проверил с 2с/5с/10с ожидания, прямой touch-маркер — не исполняется).
  - Изолированный repro: `bash -lc export ORC_SESSION=x '; echo ...'` выполняет только `export` (печатает env), тело задачи становится `$0`/позиционным — echo не выполняется. Ghostty `-e` через `open --args` не запускает многословную команду в этой версии (Ghostty 1.3.1).
  - Контраст: **Terminal-бэкенд ИСПОЛНЯЕТ ту же seam-команду** (ran.txt записан, window id 4918) — проблема именно в Ghostty-пути.
- Заявление builder «Ghostty закрывает surface при выходе -e-команды (0 husk после exit)» — **ложно в общем случае**: нашёл husk 95785 (`GHOSTTY_SPIKE`, 26 мин, команда «завершилась», процесс висел) и 9401 (`EVALFAST touch`, 26с husk). Окно самозакрывается на нормальном exit НЕ всегда; штатный путь F15 закрывает окно ТОЛЬКО через явный kill (close_ghostty).
- `pytest tests/test_ghostty.py` → 14 passed (юниты мокают os.kill/pids/spawn — реального спавна нет).
**F15: ДОРАБОТАТЬ (БЛОКЕР).** Механизм close работает на ПУСТОМ Ghostty-процессе; воркер-код через Ghostty не исполняется вовсе → бэкенд нефункционален как средство запуска.

## ПРОБЛЕМЫ

- **[БЛОКЕР]** `src/orc/spawn_ghostty.py:59-76` (spawn_ghostty) — Ghostty-спавн через `open -na Ghostty.app --args -e bash -lc '<cmd>'` НЕ исполняет команду воркера (Ghostty 1.3.1: `-e` не запускает многословную команду через `open --args`; `bash -lc` получает первый токен как `-c`-аргумент, остальное — `$0`). Нарушает F15 приёмку «воркер находится/останавливается» (воркера нет — есть пустой Ghostty), G0b «реально работает не display-only». Фикс: передавать команду одним аргументом, доказуемо исполняемым (напр. `-e /bin/bash <script-file>` или `-e bash -c "$(printf %q)"` в проверенной форме; спайк с файлом-маркером ОБЯЗАТЕЛЕН до self-pass).
- **[БЛОКЕР]** `config DEFAULTS terminal: ghostty` + `.verify/e2e-loop-close.sh` — дефолтный спавн-путь (Ghostty) сломан → M1-петля F14 разомкнулась (loop-close таймаутит). Регрессия ранее verified золотого пути. Фикс: чинить Ghostty ИЛИ дефолт `terminal` до починки; смоук M1 обязан гонять дефолтный бэкенд.
- **[БЛОКЕР]** `.verify/e2e-gate.sh` (F9) — у меня exit 1 (3 fail), builder заявил exit 0. E2E-гейт не детектится на реальном спавне (STATE.md не пишется из-за Ghostty). G2 «воркер дошёл до гейта → уведомление → ждёт живьём» не выполнен на реальном пути. Self-pass F9 недоказан.
- **[БЛОКЕР]** `.verify/kill-restart.sh:96` (F8) — рестарт-после-kill спавнит РЕАЛЬНЫЙ claude без override (жжёт окно ccusage; оставляет husk, cleanup не убирает). Нарушает задание «беречь окно» и F15 «0 husk». Фикс: override во ВСЕХ start-вызовах скрипта.
- **[СУЩ]** `src/orc/spawn.py:62-73` (worker_pid, Ghostty) — захватывает PID Ghostty-обёртки, не воркера. Формально «не None», семантически неверно. При починке спавна должен резолвить дочерний claude-PID.
- **[СУЩ]** `.verify/budget.sh:39-46` (F6) — реальная атрибуция расхода не измерена (хардкод `+12345`), только арифметика. «Расход = дельта ccusage реальна» доказано частично. Приемлемо до F12, но self-pass формулировка «live spend delta» преувеличена.

## ЧТО ПЛОХО

- **Опыт/петля**: дефолтный `orc start` на этой машине НЕ запустит ни одного работающего воркера — Ghostty поднимает пустое окно, задача висит, газета не догоняет. Для пользователя это «накидал → ничего не произошло → окна-пустышки». Хуже исходной боли husk: раньше воркер хотя бы работал.
- **Ложный зелёный self-pass**: F9/F15/F8 verify-скрипты «проходят», потому что проверяют спавн+kill Ghostty-ПРОЦЕССА, а не исполнение воркер-кода. 175 юнитов зелёные, потому что мокают спавн. Ни один тест не ловит, что Ghostty не исполняет команду — дыра в held-out проверке ровно там, где боль.
- **Заявление о Ghostty-самозакрытии не проверено на краю**: «0 husk после exit» опровергнуто двумя husk от собственных спайков builder-а. Механизм закрытия завязан на kill, а не на естественный exit, как утверждает design.
- **Относительно референса bd** (паспорт: «реально работает, grep-able»): admission/watchdog/budget — чистые, детерминированные, соответствуют планке. Спавн-слой — нет: он «выглядит зелёным», но центральная функция (запуск воркера) не исполняется.

## ПРОВЕРКА ДОКАЗАТЕЛЬСТВ

- «175 тестов, 0 регрессий» → перепрогнал: 175 passed, дважды стабильно. ВЕРНО.
- «F5 6 реальных лимит-строк CLI» → сверил с офиц. error-reference дословно + fuzz. ВЕРНО.
- «F7 0 ложных kill через in-flight маркер» → независимый real-clock тест с живым child. ВЕРНО (не хардкод).
- «F8 реальный PID (pid-None пофикшен)» → PID live, kill-0 ok. ВЕРНО буквально; НО в Ghostty это PID обёртки (СУЩ).
- «F9 GATE PASS (real notification; card; window held; resume)» → **НЕ ДОКАЗАНО = БЛОКЕР**: у меня exit 1, гейт не детектится, STATE.md не пишется.
- «F15 GHOSTTY-CLOSE PASS (0 husk)» → close работает на пустом процессе; **воркер-код не исполняется = БЛОКЕР**; заявление «0 husk после exit» опровергнуто (нашёл 2 husk).
- «Ghostty закрывает surface при выходе -e-команды» → опровергнуто (husk 95785/9401) И команда вообще не исполняется.
- «Живой claude НЕ израсходован» → ЧАСТИЧНО НЕВЕРНО: kill-restart.sh рестартом заспавнил реальный claude (pid 6777).

## СОМНЕНИЯ

- Почему builder видел F9/F15 exit 0? Вероятно недетерминизм (race sleep 3 vs Ghostty-старт) ИЛИ прогон в момент, когда что-то писало STATE.md иначе. Deterministic repro у меня — FAIL. Возможно builder гонял на Terminal-бэкенде в какой-то момент.
- Terminal-fallback исполняет команду, но `close_window` вернул `window_closed: False` — исходная husk-боль на Terminal остаётся. То есть ни один бэкенд сейчас не даёт одновременно «исполняет + чисто закрывает»: Ghostty закрывает-но-не-исполняет, Terminal исполняет-но-не-закрывает.

## РЕЗЮМЕ

F5/F6/F7 — солидны (детерминированные, реальные строки, независимо перепроверены; F6 с оговоркой по реальной дельте расхода). Но спавн-слой F15 фундаментально сломан: Ghostty (дефолт) НЕ исполняет команду воркера — поднимает пустое окно. Это каскадит: F9-гейт не детектится (E2E FAIL у меня, вопреки self-pass), F8-PID = PID обёртки, M1-петля F14 разомкнулась на дефолтном пути (регресс). Плюс kill-restart.sh жжёт реальный claude и оставляет husk. Золотой путь (F9) не работает на реальном спавне.

---

ВЕРДИКТ: ДОРАБОТАТЬ

Паспорт вкуса: PASS (газета/карточка/уведомления plain-text, сводка ПЕРВОЙ строкой, статус-глифы ✓/◐/⏸/▸ допустимы, ≤80 колонок, без «Successfully!»; доказательство — вывод budget.sh и изолированной gate-карточки выше). Оговорка: signature-опыт «утренняя газета» визуально корректен, но НЕ достигается на дефолтном пути, т.к. воркеры не работают.

Блокеры (4): (1) Ghostty-спавн не исполняет команду воркера; (2) дефолт ghostty ломает M1-петлю (регресс); (3) F9 E2E-гейт FAIL на реальном спавне; (4) kill-restart.sh жжёт claude + husk. Существенное (2): PID-семантика Ghostty; реальная дельта расхода F6 не измерена.
