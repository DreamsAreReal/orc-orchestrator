[A1-native|official-docs] Claude Code Headless Mode (claude -p)
  ЗАЯВЛЕНИЕ: Supports non-interactive execution with -p flag, session resumption (--continue, --resume <session-id>), auto-approval tools (--allowedTools), structured JSON output, exit codes for CI/CD
  ЗРЕЛОСТЬ: v2.1.205+ stable
  ПРИМЕНЕНИЕ: Headless loop: run tasks locally via CLI → capture JSON output → resume on user gate approval
  URL: https://code.claude.com/docs/en/headless.md
  ПОПРАВКА: Заявление по сути верно, два уточнения: (1) страница больше не называется «Headless Mode» — теперь это «Run Claude Code programmatically», подаётся как использование Agent SDK через CLI (claude -p); URL headless.md остаётся рабочим; (2) «exit codes for CI/CD» — не отдельная документированная фича на этой странице: exit codes упоминаются попутно (non-zero status при ошибках, превышении 10MB stdin-кэпа, невалидной --json-schema), а CI/CD — заявленный сценарий использования (рекомендуется --bare для CI/скриптов; ссылки на GitHub Actions и GitLab CI/CD).

[A1-native|official-docs] Agent SDK (query + ClaudeSDKClient)
  ЗАЯВЛЕНИЕ: Programmatic agent execution in Python/TypeScript: query() for one-off, ClaudeSDKClient for multi-turn sessions, permissionMode (dontAsk/bypassPermissions), session resume by ID
  ЗРЕЛОСТЬ: v1.0+ stable
  ПРИМЕНЕНИЕ: SDK-based orchestration: local Python/TS scheduler spawns Agent SDK instances per task, full control over permissions and sessions
  URL: https://code.claude.com/docs/en/agent-sdk/overview.md
  ПОПРАВКА: Заявление по сути верно, две уточняющие детали: (1) фрагмент цитаты про «permissionMode options: default, acceptEdits, plan, auto, dontAsk, bypassPermissions» находится не на overview.md, а на страницах permissions.md и python.md того же официального доксета (сам список значений совпадает с фактическим); (2) ClaudeSDKClient — класс только Python SDK; в TypeScript multi-turn реализуется через query() со стримингом ввода и resume, отдельного ClaudeSDKClient там нет.

[A1-native|official-docs] Routines (Cloud Scheduled Agents)
  ЗАЯВЛЕНИЕ: Autonomously-run agents on Anthropic infrastructure: Schedule trigger (cron, minimum 1 hour), API trigger (POST /fire with bearer token), GitHub event trigger (webhook), runs unattended without localhost
  ЗРЕЛОСТЬ: Research preview (v2.1+)
  ПРИМЕНЕНИЕ: Fully cloud-based autonomy: queue task → call routine API /fire endpoint → agent runs remotely. CRITICAL LIMITATION: requires claude.ai login (Max subscription), not Console API or API key
  URL: https://code.claude.com/docs/en/routines.md
  ПОПРАВКА: Заявление точное. Уточнения (не опровергают): фича в research preview ('Behavior, limits, and the API surface may change'); эндпоинт /fire работает под beta-заголовком experimental-cc-routine-2026-04-01 и доступен только claude.ai-пользователям (не Platform API); custom cron задаётся только через /schedule update в CLI (в веб-форме — пресеты); GitHub-события ограничены категориями pull_request и release; доступно на планах Pro/Max/Team/Enterprise с включённым Claude Code on the web.

[A1-native|official-docs] Session Resumption & STATE.md Persistence
  ЗАЯВЛЕНИЕ: Sessions persisted as JSONL in ~/.claude/projects/<project>/<session-id>.jsonl, resumable by ID or name. --resume <id>, --continue, /resume <name> restore full context for multi-turn recovery
  ЗРЕЛОСТЬ: Stable core feature
  ПРИМЕНЕНИЕ: Gate parking: task 1 pauses at user approval gate → session_id + stage saved in docs/STATE.md → task 2 starts parallel → task 1 resumes with --resume when gate passes
  URL: https://code.claude.com/docs/en/sessions.md
  ПОПРАВКА: Заявление точное. Нюансы (не ошибки): lookup по session ID скоупится на текущую директорию проекта и её git worktrees (из другой директории — «No conversation found»); формат JSONL-записей внутренний и меняется между версиями (парсить напрямую дока не рекомендует — использовать /export или script-интерфейсы); retention по умолчанию 30 дней (cleanupPeriodDays); путь ~/.claude переопределяется CLAUDE_CONFIG_DIR.

[A1-native|official-docs] Scheduled Tasks (/loop, CronCreate) - Session-Scoped Only
  ЗАЯВЛЕНИЕ: In-session scheduling: /loop 5m <prompt>, CronCreate tool for cron expressions, 1-minute granularity, auto-expire after 7 days. Tasks die when session ends, not for 24/7 unattended
  ЗРЕЛОСТЬ: Stable core feature
  ПРИМЕНЕНИЕ: Local in-session polling only; insufficient for inter-task orchestration or 24/7 unattended loops
  URL: https://code.claude.com/docs/en/scheduled-tasks.md
  ПОПРАВКА: Заявление точное. Один уточняющий нюанс (не ошибка): задачи не «умирают» безвозвратно при любом завершении сессии — при `claude --resume`/`--continue` неистёкшие задачи восстанавливаются (recurring в пределах 7 дней, one-shot до времени срабатывания), а backgrounding сессии переносит /loop-задачи в фоновую сессию без терминала. Полностью очищает задачи именно старт НОВОЙ беседы.

[A2-oss-orchestrators|oss-tool] ccmanager (kbwo/ccmanager)
  ЗАЯВЛЕНИЕ: TUI-менеджер множества сессий Claude Code через git worktrees и НЕСКОЛЬКО проектов из одного интерфейса, с детекцией состояния сессии (busy/waiting/idle) и status-change-hooks: можно выполнить произвольную команду, когда Claude перешёл в состояние «ждёт ввода пользователя» — то есть готовый примитив «парковка на гейте + уведомление». Умеет копировать session data Claude Code между worktrees и передавать `--resume`.
  ЗРЕЛОСТЬ: Жив: последний push 2026-07-11 (GitHub API), MIT, 1190 звёзд, всего 9 open issues (чистый backlog). Запускает установленный `claude` как есть — аутентификация остаётся у CLI, подписка Max работает без API-ключа.
  ПРИМЕНЕНИЕ: Самый прямой строительный блок для гейта: детекция Waiting + hook = «инстанс встал на утверждении ТЗ → уведомление пользователю → менеджер переключается на следующую задачу». Multi-project из одной TUI закрывает «разные проекты параллельно». Очереди НЕТ — утренний диспетчер (взять задачу из трекера, поднять worktree, запустить сессию) придётся написать поверх, но hooks дают событийную шину для него.
  URL: https://github.com/kbwo/ccmanager
  ПОПРАВКА: Существенных искажений нет. Микронюанс: README самоописывается как «CLI application» (интерактивное Ink-меню), а не буквально «TUI», но по факту это интерактивный терминальный интерфейс с меню и статус-индикаторами — слово «TUI» смысла не искажает. Живость: последний push и релиз v4.2.1 — 2026-07-11 (4 дня до даты проверки), ~1190 звёзд, не архивирован.

[A2-oss-orchestrators|oss-tool] Emdash (generalaction/emdash)
  ЗАЯВЛЕНИЕ: Десктоп-приложение (macOS brew cask) для параллельного запуска кодинг-агентов: каждая задача в своём git worktree, задачи затягиваются из Linear/GitHub/Jira и др., для Claude Code поддержаны Auto-approve, Resume и Hooks с уведомлениями об активности агента; ревью диффов, PR и merge из одного окна. Агенты берутся из локально установленных CLI.
  ЗРЕЛОСТЬ: Очень жив: YC W26, Apache-2.0, 5166 звёзд, push 2026-07-14 (коммиты ежедневно), фича «automations» активно пилится прямо сейчас (open issue «feat(automations): add stop task run action», 2026-07-14).
  ПРИМЕНЕНИЕ: Ближе всех к сценарию «утром накидал задачи в трекер»: issues из GitHub/Linear отправляются в агентов, каждый в изолированном worktree, Claude Code — через локальный CLI (подписка Max, без API-ключа), Resume есть. Но диспетч сегодня ручной (кликнуть «в агента»), авто-очереди и сериализации по проекту нет — либо ждать «automations», либо дергать его как исполнителя из своего диспетчера.
  URL: https://github.com/generalaction/emdash
  ПОПРАВКА: Коррекция не требуется. Единственная микро-неточность: цитата ресерчера про Hooks усечена — полная формулировка в доке: «...supports notifications with lifecycle hooks, when the agent is working, awaiting input, or done»; смысл не искажён. Проект жив: последний релиз v1.1.39 от 2026-07-14.

[A2-oss-orchestrators|oss-tool] Bernstein (sipyourdrink-ltd/bernstein)
  ЗАЯВЛЕНИЕ: Детерминированный Python-планировщик (не LLM) над CLI-агентами: декомпозиция цели → параллельные агенты в изолированных git worktrees (один на задачу) → «janitor»-гейты (тесты/линт/типы) → merge queue, сериализующий вливания. Есть парковка/эскалация при сталле воркера, crash-safe resume по hash-chained ledger, detached run-service (submit → отключился → attach позже), plan-approval гейт для спеки и учёт расхода по пулам `api`/`subscription`. In-process гейты реализованы через хуки Claude Code (Stop, PreToolUse).
  ЗРЕЛОСТЬ: Молодой, но активный: создан 2026-03-22, push 2026-07-14, Apache-2.0, 672 звезды, pipx/PyPI. Один вендор (sipyourdrink-ltd); README чрезвычайно перегружен фичами (аудит-цепочки, C2PA, SPIFFE) — заявленная поверхность заметно шире, чем подтверждает размер сообщества, обязательно пилотировать перед доверием.
  ПРИМЕНЕНИЕ: Идеологически ближе всех к конвейеру пользователя: детерминированная оркестрация, verify-гейты, park/escalate на сталле, resume после падения из журнала (аналог роли STATE.md), submit/detach/attach ≈ очередь, worktree на задачу + merge queue закрывают инвариант «один пишущий в репозиторий». Слабое место: его гейты машинные (tests/lint), а пользовательский гейт утверждения ТЗ есть только на этапе plan compile/approve — стыковку со скилл-конвейером в ~/.claude придётся проектировать.
  URL: https://github.com/sipyourdrink-ltd/bernstein
  ПОПРАВКА: Коррекция не требуется. Единственный нюанс (не искажение): планировщик детерминированный, но одна LLM-вызов всё же используется — для первичной декомпозиции цели («Bernstein does one LLM call to break down your goal, then the rest … is plain Python»), что соответствует формулировке заявления «декомпозиция цели → …».

[A2-oss-orchestrators|oss-tool] Happy (slopus/happy)
  ЗАЯВЛЕНИЕ: Обёртка `happy` вместо `claude`: сессия Claude Code зеркалируется на телефон/веб с end-to-end-шифрованием, приходят push-уведомления, когда Claude Code нужно разрешение или он упал в ошибку; управление можно перехватить с телефона и вернуть на компьютер одним нажатием клавиши.
  ЗРЕЛОСТЬ: Жив: MIT, 22632 звезды, push 2026-07-11 (GitHub API). Оборачивает локальный CLI — аутентификация/подписка Max сохраняются как есть.
  ПРИМЕНЕНИЕ: Не оркестратор, а недостающий слой «уведомление на гейте + удалённый ответ»: инстанс паркуется на утверждении ТЗ → push на телефон → пользователь утвердил из любого места → конвейер поехал дальше. Прямая замена архивированному Omnara. Комбинируется с любым диспетчером: достаточно, чтобы тот запускал `happy` вместо `claude`.
  URL: https://github.com/slopus/happy
  ПОПРАВКА: Заявление точное. Микроуточнение: проект теперь поддерживает не только Claude Code, но и Codex («happy codex»), и в актуальном README команда запуска показана как «happy claude»/«happy» — по смыслу это та же обёртка. Проект жив: релиз cli-1.1.10 от 2026-06-23, 22.6k звёзд.

[A2-oss-orchestrators|oss-tool] Claude Squad (smtg-ai/claude-squad)
  ЗАЯВЛЕНИЕ: Терминальный менеджер нескольких AI-агентов: tmux-сессия на агента + git worktree на сессию (каждая на своей ветке), выполнение задач в фоне с yolo/auto-accept-режимом, pause/resume сессий, ревью диффов и push прямо из TUI.
  ЗРЕЛОСТЬ: Жив, но темп умеренный: AGPL-3.0, 8116 звёзд, 19 релизов, последний push 2026-06-17. Требует tmux и gh. Известные грабли из issues: «Enable multiple git repos with claude squad» открыт с 04.2025 — инструмент по сути одно-репозиторный (запускается внутри одного проекта); tmux-хрупкость («Error captureing pane content after starting cs», «timed out waiting for tmux session»).
  ПРИМЕНЕНИЕ: Хорош для «задачи одного проекта — параллельно/фоном в worktrees», но мульти-проектность из коробки не закрывает: для «разные проекты параллельно» придётся поднимать несколько экземпляров (по одному на репозиторий) — это и есть его главный конфликт с контуром пользователя. Auth: запускает локальный `claude` → подписка Max работает (API-ключ в README требуется только для Codex).
  URL: https://github.com/smtg-ai/claude-squad
  ПОПРАВКА: Не требуется. Единственный нюанс терминологии: «pause» в меню называется «Checkout» (коммитит изменения и ставит сессию на паузу), resume — отдельная клавиша «r». Дата релиза v1.0.19 = 2026-06-17 подтверждена GitHub API (published_at 2026-06-17T14:47:44Z), проект активен (pushed_at 2026-06-17, archived=false).

[A2-oss-orchestrators|oss-tool] Vibe Kanban (BloopAI/vibe-kanban)
  ЗАЯВЛЕНИЕ: Kanban-доска над коднг-агентами: задачи планируются как issues, затем исполняются агентами в изолированных workspaces (git worktree-изоляция, есть runtime-флаг управления очисткой worktree), ревью диффов, встроенный preview и PR — по форме это точное совпадение с хотелкой «очередь → исполнение». НО: компания bloop закрылась 10.04.2026, облачные сервисы (kanban issues, комментарии, проекты) удалены, остался локальный режим и community-ماintenance.
  ЗРЕЛОСТЬ: Полумёртв: Apache-2.0, 27376 звёзд, но последний push 2026-04-24 (совпадает с shutdown-релизом v0.1.44), 534 open issues без хозяина. Community-maintenance пока не материализовался в коммиты.
  ПРИМЕНЕНИЕ: Брать в ЦЕНТР нового контура рискованно: развитие остановлено, облачная часть (как раз kanban-очередь issues) выпилена. Годится как short-term локальное решение или донор паттернов (модель task→workspace→worktree→PR) для собственного диспетчера. Auth — через локально настроенные CLI агентов (подписка).
  URL: https://github.com/BloopAI/vibe-kanban
  ПОПРАВКА: Заявление точное. Нюансы для полноты: (1) «community-maintenance» — пока обещание из shutdown-поста; фактически последний коммит и релиз (v0.1.44) датированы 24.04.2026, т.е. ~3 месяца видимой активности нет, на репозитории баннер «Vibe Kanban is sunsetting»; (2) облачные сервисы работали ещё 30 дней после 10.04.2026 (отключение ~10.05.2026), сейчас уже удалены — на текущую дату заявление верно.

[A2-oss-orchestrators|official-docs] Conductor (conductor.build, Melty Labs) — закрытый исходник, но эталон auth-модели
  ЗАЯВЛЕНИЕ: Бесплатный Mac-app: «параллельные Claude Code/Codex/Cursor-агенты в изолированных workspaces на вашем Mac»; официальная дока прямо документирует режим CLI-auth от подписки Claude Pro/Max (без API-ключа) и предупреждает, что выставленный ANTHROPIC_API_KEY переключит биллинг на API. НЕ open source — включён как рабочая альтернатива и как официальное подтверждение легитимности схемы «оркестратор поверх Max-подписки».
  ЗРЕЛОСТЬ: Активен: версия 0.75.0 на сайте (июль 2026), закрытый исходник, бесплатен (bring-your-own-subscription), есть Enterprise-тир.
  ПРИМЕНЕНИЕ: Если OSS не принципиален — самый «маковский» готовый вариант параллельных инстансов от подписки Max. Ограничение для контура: закрытый код — нельзя встроить свой диспетчер очереди, сериализацию по проекту и парковку на гейте ТЗ глубже, чем позволяет UI. Полезен минимум как документированное доказательство, что схема «менеджер инстансов поверх CLI-логина Max» — штатная.
  URL: https://www.conductor.build/docs/reference/harnesses/claude-code
  ПОПРАВКА: Косметическая правка: цитата с главной страницы у ресерчера склеена в одну фразу; на самом сайте это заголовок «Run parallel coding agents on your Mac.» и отдельный подзаголовок «Create parallel Claude Code, Codex, and Cursor agents in isolated workspaces.» (глагол Create, а не Run). Смысл не искажён. Бесплатность подтверждается не самой докой (там цен нет), а страницей загрузки/обзорами: приложение бесплатное, оплачивается только собственная подписка/API-ключи пользователя.

[A2-oss-orchestrators|oss-tool] cmux (manaflow-ai/cmux)
  ЗАЯВЛЕНИЕ: Нативный macOS-терминал на движке Ghostty, заточенный под много параллельных коднг-агентов: вкладки/сплиты на агента, визуальная и десктопная сигнализация «агенту нужно внимание» (синее кольцо на панели, подсветка вкладки, текст последнего уведомления в сайдбаре), встроенный scriptable-браузер для агентов, восстановление сессий. Это слой наблюдаемости, а не очередь и не worktree-менеджер.
  ЗРЕЛОСТЬ: Очень жив: 24484 звезды, push 2026-07-14 (GitHub API), GPL-3.0-or-later. Не путать с одноимённым craigsc/cmux («tmux for Claude Code», 583 звезды, послабее).
  ПРИМЕНЕНИЕ: Кабина пилота для контура на Mac: все инстансы (запущенные вашим диспетчером) живут во вкладках, «агент ждёт ввода» виден сразу — человеческий фронтенд для парковки на гейте, когда пользователь за компьютером (Happy закрывает случай «вне компьютера»). Агенты аутентифицируются сами (локальный `claude` → Max). Очередь и worktrees остаются на диспетчере.
  URL: https://github.com/manaflow-ai/cmux
  ПОПРАВКА: Не требуется. Единственный нюанс: GitHub API помечает лицензию как NOASSERTION (кастомный текст с оговоркой о коммерческой лицензии по запросу), но формулировка «GPL-3.0-or-later» дословно присутствует в README, на который и ссылается ресерчер.

[A3-task-trackers|oss-tool] beads (bd) — agent-native issue tracker Стива Йегги
  ЗАЯВЛЕНИЕ: Трекер, спроектированный под агентов: граф зависимостей задач, `bd ready` выдаёт задачи без открытых блокеров, `bd update <id> --claim` АТОМАРНО забирает задачу (ставит assignee + in_progress), статусы open/in_progress/blocked/closed (blocked = 'Waiting on dependencies'), приоритеты P0-Pn, хэшовые ID против merge-конфликтов при мультиагентной работе, `bd setup claude` ставит интеграцию в Claude Code (в репо есть .claude-plugin и AGENT_INSTRUCTIONS.md).
  ЗРЕЛОСТЬ: Очень живой: 25.3k звёзд, релиз v1.1.0 от 2026-07-04 (93-й релиз), последний пуш 2026-07-14 (вчера). Хранение — встроенный Dolt в .beads/ (версионируемая SQL-БД, single writer), .beads/issues.jsonl — экспорт для обмена.
  ПРИМЕНЕНИЕ: Прямое попадание в контур: утром `bd create -p 1 ...` + `bd dep add` по 10 задачам; оркестратор в цикле дергает `bd ready --json`, атомарно `--claim` и поднимает headless Claude Code в workspace нужного проекта. Гейт ТЗ моделируется блокирующей задачей «approve ТЗ» на человеке: пока она open, зависимая build-задача не попадает в ready — парковка на гейте и переключение на следующую получаются бесплатно из ready-семантики. Сериализация внутри проекта — blocks-цепочка между задачами одного репо; BEADS_DIR/пер-репо .beads покрывают мультипроектность. Claim атомарен даже при нескольких инстансах — снимает гонки за задачу.
  URL: https://github.com/steveyegge/beads
  ПОПРАВКА: Заявление по сути точно. Два минорных уточнения: (1) репозиторий перенесён в организацию — каноничный адрес теперь https://github.com/gastownhall/beads, старый URL steveyegge/beads редиректит и остаётся рабочим; (2) статусов пять, а не четыре: кроме open/in_progress/blocked/closed есть ещё deferred («Scheduled for later»). Проект активно жив: последний push 2026-07-14, релиз v1.1.0 от 2026-07-04, ~25,3k звёзд.

[A3-task-trackers|official-docs] GitHub Issues как очередь: @claude + claude-code-action + OAuth-токен подписки
  ЗАЯВЛЕНИЕ: Официальный механизм Anthropic: меншен @claude в любом issue/PR запускает Claude Code; action принимает `claude_code_oauth_token` как альтернативу API-ключу, а `claude setup-token` генерирует долгоживущий OAuth-токен от подписки специально «for CI and scripts» — т.е. очередь на GitHub Issues совместима с Max-подпиской без API-биллинга.
  ЗРЕЛОСТЬ: Официальный продукт Anthropic, v1 GA (breaking changes с беты задокументированы), доки актуальны на июль 2026. Нюанс: в README/FAQ action subscription-auth не рекламируется — поддержка подтверждается полем в action.yml (проверено в коде main-ветки).
  ПРИМЕНЕНИЕ: Issues = очередь без нового софта: labels = приоритет, assignee = lease, label «awaiting-human» = статус гейта, репо = проект → мультипроектность и сериализация «один репо — один воркер» нативны. Для локального Mac предпочтителен не облачный Action, а локальный поллер (`gh issue list --label ready`) + headless Claude Code от локальной подписочной авторизации; Action с oauth-токеном — запасной облачный вариант. Уведомление на гейте = комментарий/label → штатные GitHub-нотификации на телефон. Восстановление: issue остаётся assigned+in-progress, резюм по docs/STATE.md workspace-а.
  URL: https://code.claude.com/docs/en/github-actions
  ПОПРАВКА: Заявление подтверждено; две уточняющие детали: (1) сама страница github-actions документирует только anthropic_api_key — путь через claude_code_oauth_token документирован в action.yml репозитория и на странице authentication (токен ставится как CLAUDE_CODE_OAUTH_TOKEN, живёт 1 год, scoped to inference only); (2) подписка не обязательно Max — подходит Pro, Max, Team или Enterprise. Репозиторий anthropics/claude-code-action жив (последний push 2026-07-14, не архивирован).

[A3-task-trackers|oss-tool] Backlog.md — markdown-очередь внутри git-репо
  ЗАЯВЛЕНИЕ: Markdown-нативный таск-менеджер: каждая задача — .md файл в репо, есть milestones и dependencies (порядок исполнения ревьюится), терминальный (`backlog board`) и веб-канбан, встроенная интеграция с агентами: инструкция `backlog instructions overview` для CLI-агентов и опциональный MCP-коннектор с автонастройкой Claude Code.
  ЗРЕЛОСТЬ: Активный: 6.2k звёзд, v1.48.0 от 2026-07-12, последний пуш 2026-07-14, MIT. Частые релизы.
  ПРИМЕНЕНИЕ: Философия «очередь живёт в самом репо» идеально совместима с инвариантами пользователя: статус задачи коммитится вместе с кодом (git-коммиты как внешняя память), восстановление после падения — из тех же файлов, что docs/STATE.md; конфликт записи исключён инвариантом «один пишущий инстанс на репо». Гейт = отдельный статус/колонка «Awaiting approval» (статусы настраиваются). Минусы: нет атомарного lease (не нужен при одном писателе) и нет кросс-репо очереди — утренние 10 задач по разным проектам придётся раскидывать по репо или держать отдельный хаб-борд, из которого оркестратор диспетчеризует.
  URL: https://github.com/MrLesk/Backlog.md
  ПОПРАВКА: Не требуется: все цитаты совпадают с README дословно. Дополнение по живости: последний релиз v1.48.0 — 2026-07-12, последний коммит — 2026-07-14, репозиторий не архивирован, ~6.2k звёзд.

[A3-task-trackers|official-docs] Linear как очередь: официальный MCP-сервер + делегация issue агентам
  ЗАЯВЛЕНИЕ: Официальный hosted MCP-сервер Linear подключается к Claude Code одной командой и даёт поиск/создание/обновление issues; в агентном API Linear issue делегируются агенту (delegate, не assignee — владение остаётся у человека), а вебхук AgentSessionEvent `created` служит триггером взятия задачи.
  ЗРЕЛОСТЬ: Официальный сервер Linear (mcp.linear.app), OAuth 2.1 с dynamic client registration + Bearer-токены; агентная платформа (linear.app/developers/agents) — действующее направление продукта, доки актуальны на 2026.
  ПРИМЕНЕНИЕ: Лучший вариант, если хочется «взрослый» трекер с мобильными пуш-уведомлениями на гейте: кастомный workflow-статус «Awaiting approval» паркует задачу, пользователь утверждает ТЗ из телефона. Оркестратор на Mac поллит статусы через MCP/GraphQL (полная делегация через AgentSession требует регистрации собственного agent-приложения — тяжелее, для соло-пользователя избыточно). Забор задачи = перевод в In Progress + assignee; не CAS, но при единственном оркестраторе гонок нет. Мультипроектность — проекты/команды Linear поверх любых репо.
  URL: https://linear.app/docs/mcp
  ПОПРАВКА: Коррекции не требуется. Мелкие уточнения: агентные цитаты находятся на https://linear.app/developers/agents (не на URL кандидата linear.app/docs/mcp); «одной командой» = одна команда подключения, после неё нужен шаг /mcp для OAuth-авторизации; после вебхука created агент обязан ответить `thought`-активностью в течение 10 секунд.

[A3-task-trackers|oss-tool] Task Master (claude-task-master) — выбор следующей задачи по зависимостям
  ЗАЯВЛЕНИЕ: Трекер для AI-разработки: декомпозиция PRD в tasks.json с зависимостями, `task-master next` выбирает следующую доступную задачу; официально заявлена работа через Claude Code CLI БЕЗ API-ключа (модели claude-code/opus, claude-code/sonnet — использует локальную подписочную авторизацию); теги для параллельных workstream-ов; MCP-интеграция.
  ЗРЕЛОСТЬ: 27.9k звёзд, но последний пуш 2026-04-28 (~2.5 месяца тишины на момент проверки), документация переехала на tryhamster.com — признаки ребрендинга/замедления. Перед ставкой на него проверить живость ещё раз.
  ПРИМЕНЕНИЕ: Ценен как прецедент: доказывает паттерн «трекер сам гоняет LLM-вызовы через подписочный Claude Code без API-ключа», который стоит воспроизвести в собственном оркестраторе. В контур встаёт хуже beads: заточен на один проект (tasks.json в репо), статуса «ждёт человека» из коробки нет, атомарного claim нет — плюс риск заброшенности. Брать идеи (next-по-зависимостям, tags), не сам инструмент.
  URL: https://github.com/eyaltoledano/claude-task-master
  ПОПРАВКА: Заявление точное. Микро-уточнения: «tasks.json» буквально упомянут не в README, а в docs/task-structure.md того же репозитория (фактическое хранилище — .taskmaster/tasks/tasks.json); пользовательская документация переехала на tryhamster.com. Проект жив: последний push 2026-04-28, релиз 0.43.1 от 2026-03-31, 27.9k звёзд, не archived.

[A4-scheduling|official-docs] Единый пул лимитов подписки (5-часовое окно + два недельных капа) на все сессии и поверхности
  ЗАЯВЛЕНИЕ: На Max x20 параллельные инстансы Claude Code НЕ умножают квоту: все сессии на Mac, claude.ai и Desktop тратят один общий пул; помимо 5-часового окна у Max два недельных капа — по всем моделям и отдельно по Sonnet, ресет в фиксированное время, назначенное аккаунту. Параллелизм лишь ускоряет сжигание общего окна.
  ЗРЕЛОСТЬ: Help Center актуален на июль 2026. Подтверждено также: "Max plans also have two weekly usage limits: one that applies across all models and another for Sonnet models only" и "Weekly limits reset at a fixed time each week that is assigned to your account" (support.claude.com/en/articles/11049741). Свежий буст: 2026-05-06 Anthropic удвоила 5-часовые лимиты Claude Code для Pro/Max/Team и сняла peak-hours reduction — "doubling Claude Code's five-hour rate limits for Pro, Max, Team, and seat-based Enterprise plans" (anthropic.com/news/higher-limits-spacex).
  ПРИМЕНЕНИЕ: Диспетчер очереди должен вести ОДИН глобальный бюджет на все воркеры, а не per-instance. Задачи, запаркованные на гейте ТЗ, токены не жгут — их можно держать сколько угодно; ограничивать надо только число АКТИВНО работающих инстансов. Недельный кап — отдельный счётчик: упор в weekly паркует всю очередь до Mon-ресета, упор в session — только до конца 5-часового окна.
  URL: https://support.claude.com/en/articles/11647753-how-do-usage-and-length-limits-work
  ПОПРАВКА: Заявление по сути верно, но требует правки источников: цитируемый URL (11647753) подтверждает ТОЛЬКО общий пул по поверхностям и опции при исчерпании; 5-часовое окно, два недельных капа и фиксированный назначенный аккаунту ресет живут в других официальных статьях — добавить ссылки на https://support.claude.com/en/articles/11049741-what-is-the-max-plan (два недельных капа: все модели + отдельно Sonnet; фиксированный ресет) и https://support.claude.com/en/articles/9797557-usage-limit-best-practices (five-hour session limit). Оговорки: (1) в доках Anthropic внутреннее противоречие — 9797557 в описании UI говорит про недельный ресет «for Opus only and all other models», тогда как каноническая статья Max-плана говорит «Sonnet models only»; формулировку «отдельно по Sonnet» оставить, но со ссылкой именно на 11049741; (2) тезис «параллельные инстансы не умножают квоту, лишь ускоряют сжигание окна» — корректный вывод из общего per-account пула, но дословно в доках не сформулирован (подавать как следствие, не как цитату).

[A4-scheduling|official-docs] Точные строки исчерпания лимита + отличение планового лимита от 429/529 (Claude Code errors reference)
  ЗАЯВЛЕНИЕ: Claude Code выдаёт три детерминированные строки при упоре в лимит подписки, каждая со временем ресета: session, weekly и Opus-only; блокирует запросы до ресета. 429/529 — НЕ плановый лимит (капасити-проблемы) и различаются по отсутствию quota-заголовков — их надо ретраить, а не парковать очередь.
  ЗРЕЛОСТЬ: Официальная дока code.claude.com, актуальна на июль 2026 (упоминает версии v2.1.2xx).
  ПРИМЕНЕНИЕ: Это готовая спека парсера back-pressure для конвейера: воркер-обёртка матчит три строки в выводе headless-прогона → парсит время ресета → session-лимит = парковка активных задач до конца окна, weekly = глубокая парковка всей очереди, Opus-лимит = деградация на Sonnet и продолжение. 429/529 → экспоненциальный retry без остановки очереди.
  URL: https://code.claude.com/docs/en/errors
  ПОПРАВКА: Заявление точное. Единственный минорный нюанс: фраза про отсутствие "unified quota headers" в доке привязана к секции временного 429-троттлинга ("Server is temporarily limiting requests"); для 529 различение с плановым лимитом сформулировано отдельно ("is not your usage limit and doesn't count against your quota"). Также с v2.1.199 такие 429 ретраятся автоматически при любом способе входа (раньше — только API key/Enterprise).

[A4-scheduling|official-docs] Headless-режим: машинные события rate_limit (stream-json system/api_retry) + session_id для резюма
  ЗАЯВЛЕНИЕ: В headless (`claude -p --output-format stream-json`) Claude Code эмитит событие system/api_retry с категорией error (в т.ч. `rate_limit`, `overloaded`, `billing_error`) и полем retry_delay_ms — ранний машинный сигнал back-pressure ещё ДО фейла прогона; `--output-format json` возвращает session_id для последующего `--resume`, причём поиск session_id скоупится каталогом проекта и его git worktree.
  ЗРЕЛОСТЬ: Официальная дока, актуальна на июль 2026 (описывает поведение до/после v2.1.208).
  ПРИМЕНЕНИЕ: Транспорт воркера в контуре: гнать задачи через stream-json, слушать api_retry(error=rate_limit) как метрику давления (растёт частота ретраев → снизить параллелизм ДО жёсткой блокировки), из финального result забирать session_id и класть в запись задачи очереди — резюм после парковки/падения делать `--resume <id>` из того же workspace-каталога (совместимо с docs/STATE.md-резюмом конвейера).
  URL: https://code.claude.com/docs/en/headless
  ПОПРАВКА: Не требуется. Единственный нюанс: страница по URL /docs/en/headless теперь называется "Run Claude Code programmatically" (headless-режим позиционируется как Agent SDK через CLI `claude -p`), но содержимое полностью подтверждает заявление; событие api_retry описано в контексте `--output-format stream-json`, как и указано в кандидате.

[A4-scheduling|official-docs] /usage как гейдж плановых лимитов у подписчика (страница Manage costs) — с оговорками
  ЗАЯВЛЕНИЕ: Подписчик Max видит в /usage план-бары (5-часовое окно + недельные капы) и разбивку потребления, но разбивка считается из ЛОКАЛЬНОЙ истории сессий этой машины (другие устройства и claude.ai не учтены), а сам usage-endpoint бывает rate-limited (тогда показывается last-known снапшот до 60 минут давности) — точный проактивный учёт на /usage не построить. Там же: agent teams ≈ 7x токенов обычной сессии.
  ЗРЕЛОСТЬ: Официальная дока, актуальна на июль 2026 (фичи v2.1.174–v2.1.208).
  ПРИМЕНЕНИЕ: Для контура вывод двоякий: (а) /usage — интерактивная команда, в headless-очередь её не встроить как API, и её данные частично локальны → строить учёт комбинированно: локальный гейдж (ccusage) + реактивный парсинг строк лимита; (б) правило 7x — аргумент НЕ использовать agent teams внутри воркеров очереди: квоту Max x20 экономнее тратить на несколько независимых одиночных инстансов, чем на команды.
  URL: https://code.claude.com/docs/en/costs
  ПОПРАВКА: Две оговорки: (1) цифра 7x в доке квалифицирована условием «when teammates run in plan mode» — в заголовке кандидата квалификатор опущен, обобщать на любые agent teams нельзя (в цитате ресерчера квалификатор есть); (2) формулировка «план-бары (5-часовое окно + недельные капы)» — синтез: секция /usage говорит про «plan usage bars» и «plan limits» без расшифровки окон; про rolling five-hour window + weekly window та же страница говорит в секции Teams/Enterprise и в описании limit-сообщений («session limit» / «weekly limit»). По смыслу верно, но это не дословный текст секции /usage.

[A4-scheduling|official-docs] Официальный паттерн параллельных сессий на одной машине: claude --worktree
  ЗАЯВЛЕНИЕ: Anthropic официально рекомендует параллелить сессии на одной машине через git worktree (`claude --worktree <name>`): каждая сессия — изолированный checkout на своей ветке, правки не сталкиваются; мониторинг нескольких сессий с одного экрана — background agents (agent-view).
  ЗРЕЛОСТЬ: Официальная дока, актуальна на июль 2026; отдельная страница /en/worktrees (cleanup, .worktreeinclude).
  ПРИМЕНЕНИЕ: Прямо ложится на инвариант пользователя «в один репозиторий пишет один инстанс»: планировщик сериализует задачи по project_id (одна активная задача на репо), а разные проекты гонит параллельно в своих каталогах. Если позже захочется 2 задачи одного проекта одновременно — официальный --worktree даёт изоляцию без нарушения инварианта (один пишущий на checkout), git-коммиты как внешняя память сохраняются per-branch.
  URL: https://code.claude.com/docs/en/common-workflows
  ПОПРАВКА: 

[A4-scheduling|oss-tool] ccusage — локальный гейдж 5-часовых окон и burn rate (ryoppippi/ccusage)
  ЗАЯВЛЕНИЕ: CLI читает локальные JSONL Claude Code и отдаёт отчёты по дням/неделям/сессиям; команда `ccusage blocks` трекает именно 5-часовые биллинг-окна с live-мониторингом активного блока — готовый источник «сколько окна осталось» для функции допуска задач в очередь.
  ЗРЕЛОСТЬ: Очень живой: 17.2k звёзд, 131 релиз, последний v20.0.17 от 2026-07-10 (5 дней назад), 1473 коммита, MIT, переписан на Rust.
  ПРИМЕНЕНИЕ: Демон-гейдж контура: перед диспатчем следующей задачи опрашивать `ccusage blocks` (остаток активного 5-часового блока + burn rate) и решать, сколько воркеров допустить; при приближении к капу — превентивно не стартовать новые задачи (мягкий back-pressure до жёсткой блокировки). Альтернатива с предсказаниями исчерпания — Claude-Code-Usage-Monitor (8.4k звёзд, v4.0.0 06/2026, MIT), но его токен-лимиты планов (~220k для Max20) — community-оценки, не официальные числа.
  URL: https://github.com/ryoppippi/ccusage
  ПОПРАВКА: Две уточняющие поправки: (1) «live-мониторинг» устарел терминологически — режим `blocks --live` удалён в v18.0.0 (текущая версия v20.0.17); мониторинг активного блока теперь снапшотом через `ccusage blocks --active` (время до конца 5-часового окна, burn rate, прогноз), а для real-time рекомендован `ccusage statusline`. Для функции «сколько окна осталось» использовать `blocks --active` (+ `--json`), не `--live`. (2) Репозиторий переехал в org ccusage/ccusage (старый URL редиректит), и тул стал мультиисточниковым (Claude Code, Codex, Gemini CLI и др.), а не только «JSONL Claude Code» — данные Claude Code читаются из ~/.claude/ и ~/.config/claude/projects/ (JSONL).

[A4-scheduling|oss-tool] claude-auto-resume — референс парковки до восстановления окна (terryso/claude-auto-resume)
  ЗАЯВЛЕНИЕ: Скрипт детектит в выводе Claude CLI машинный паттерн лимита с epoch-таймстампом ресета, считает время ожидания, показывает countdown и автоматически возобновляет работу (`claude -c -p ...`) после восстановления окна — рабочий образец механики «детект лимита из headless-вывода → парковка → авто-резюм».
  ЗРЕЛОСТЬ: 796 звёзд, MIT, bash; активность умеренная (32 коммита в develop) — брать как референс паттерна, не как зависимость. Встроенного авто-резюма в Claude Code НЕТ — это подтверждают открытые feature-requests в anthropics/claude-code (#36320, #62788, #26775). Паттерн строки стоит перепроверить на текущем CLI: интерактивный текст в доках уже "You've hit your session limit · resets …".
  ПРИМЕНЕНИЕ: В контуре пользователя воркер-обёртка повторяет эту механику, но лучше: вместо слепого "continue" — резюм конвейера по docs/STATE.md конкретной задачи; вместо --dangerously-skip-permissions — allowlist/permission-mode (полная автономия только ПОСЛЕ гейта ТЗ). Epoch-таймстамп из сообщения — точное время снятия парковки для планировщика.
  URL: https://github.com/terryso/claude-auto-resume
  ПОПРАВКА: Заявление верно, одно уточнение: пайп-формат "Claude AI usage limit reached|<epoch>" — легаси («old format»); скрипт сейчас детектит ТРИ формата вывода Claude CLI, включая человекочитаемые "X-hour limit reached ∙ resets Xam/pm" и "You've hit your limit · resets 2am (Europe/Paris)" с парсингом таймзоны (grep -E "(Claude AI usage limit reached|limit reached.*resets|hit your limit.*resets)"). Живость подтверждена: последний push 2026-05-09, не архивирован, 796 звёзд; сам README честно предупреждает, что скрипт зависит от формата вывода Claude CLI и может ломаться при его изменении.

[A4-scheduling|practitioner] Память на инстанс Claude Code на macOS (GitHub issues anthropics/claude-code)
  ЗАЯВЛЕНИЕ: Реальный футпринт одного инстанса Claude Code на Mac — единицы гигабайт с ростом на длинных сессиях: пользователи фиксируют 2.5–8 GB на инстанс (M4 Pro), RSS ~4 GB после ~3 часов, лейки до 12 GB и экстремально 108 GB. Практическая планка для очереди: 2–4 одновременных инстанса на Mac с 16–32 GB RAM плюс обязательный рецикл долгоживущих процессов.
  ЗРЕЛОСТЬ: Официальных цифр Anthropic по памяти нет; это практика из трекера вендора. #8968 (10/2025, closed as not planned); свежие подтверждения тренда: #37240 (03/2026, RSS ~4GB после 3 ч на 16GB Mac), #23252 (02/2026, ~12GB), #56960 (05/2026, лейк до 108GB на M5 Max). Проблема воспроизводится через версии — закладывать в дизайн.
  ПРИМЕНЕНИЕ: Формула допуска планировщика: max_workers = min(бюджет_лимита, floor(свободная_RAM/5GB), число_проектов_в_очереди); на типовом 32GB Mac — 3-4 воркера. Вотчдог по RSS: при превышении порога — kill инстанса и рестарт через `--resume`/docs/STATE.md (восстановление после падения у пользователя уже спроектировано, тот же путь покрывает и рецикл по памяти).
  URL: https://github.com/anthropics/claude-code/issues/8968
  ПОПРАВКА: Заявление в целом точно, три уточнения: (1) в цитате ресерчера пропущено слово "is" — в оригинале "each instance of claude code IS using anywhere from..."; (2) числа 4 GB RSS/3 часа, 12 GB и 108 GB взяты не из #8968, а из других issues того же репозитория (#37240, #23252/#21923, #56960 соответственно) — заявление корректно как агрегат по "GitHub issues" (мн.ч.), но URL указывает только на один; (3) #8968 закрыт как "not planned" (autoclose), т.е. числа — пользовательские репорты без подтверждения Anthropic, а «планка 2–4 инстанса + рецикл» — собственный вывод практика, в первоисточнике её нет (хотя она арифметически согласуется с данными).

[A5-autonomy-horizon|paper] METR Time Horizon 1.1 — свежие замеры 50%-горизонта frontier-моделей (январь 2026)
  ЗАЯВЛЕНИЕ: Актуальный 50%-time-horizon: Claude Opus 4.5 = 320 минут человеко-работы (CI [170, 729]), GPT-5 = 214 мин, o3 = 121 мин, Claude Opus 4 = 101 мин; темп удвоения горизонта после 2023 года — 131 день (после 2024 — 88.6 дней). Сьют расширен до 228 задач, длинных (8+ ч) — 31; замеры выше 16 часов ненадёжны.
  ЗРЕЛОСТЬ: Отчёт METR от 2026-01-29, страница metr.org/time-horizons/ обновлена 2026-05-08 — самые свежие первичные числа; не рецензировано, но это первоисточник самой метрики
  ПРИМЕНЕНИЕ: Калибровка размера «смены»: текущий frontier держит 50% успеха на ~5.3 ч человеко-эквивалентной работы. Для контура очередь→Claude Code это верхняя граница одной задачи целиком; рабочие майлстоуны надо резать сильно короче (см. правило 5x ниже). Темп удвоения ~131 день значит: калибровку размера смены пересматривать при каждой смене модели, зашить как конфиг, не константу.
  URL: https://metr.org/blog/2026-1-29-time-horizon-1-1/
  ПОПРАВКА: Коррекций по существу нет. Две микро-детали: (а) цитата в кандидате обрезана — в оригинале «...20% more rapid under TH1.1»; (б) в основном тексте поста пост-2024 удвоение округлено до 89 дней, точное значение 88.6 дней стоит в сравнительной таблице — кандидат корректно взял табличное значение.

[A5-autonomy-horizon|paper] METR — Measuring AI Ability to Complete Long Tasks (Kwa et al., arXiv 2503.14499)
  ЗАЯВЛЕНИЕ: Каноническое определение метрики: 50%-горизонт — длительность (в человеко-времени) задач, которые агент решает с 50% успеха; горизонт удваивается ~каждые 7 месяцев с 2019; 80%-горизонт примерно в 5 раз короче 50%-го — это ключевое число для планирования надёжных кусков работы.
  ЗРЕЛОСТЬ: Март 2025, широко цитируемая базовая работа; числа конкретных моделей устарели (Claude 3.7 = 50 мин), но методология актуальна — TH1.1 является её прямым обновлением
  ПРИМЕНЕНИЕ: Правило перевода: хочешь 80% успеха вместо 50% — дели горизонт на ~5. При TH1.1-горизонте Opus 4.5 в 320 мин это ≈60-100 мин человеко-работы на майлстоун. Практика для конвейера: после гейта ТЗ резать build-фазу на майлстоуны ~1-2 ч человеко-эквивалента, каждый с внешней верификацией и коммитом.
  URL: https://arxiv.org/abs/2503.14499
  ПОПРАВКА: Мелкое уточнение: актуальное название статьи на arXiv — "Measuring AI Ability to Complete Long Software Tasks" (в кандидате — вариант ранней версии без слова "Software"). Также в теле статьи разброс для 80%-горизонта указан как 4-6x короче ("roughly 5x" — формулировка абстракта), и абстракт оговаривает, что тренд удвоения мог ускориться в 2024.

[A5-autonomy-horizon|paper] METR — Clarifying limitations of time horizon (заметка 2026-01-22)
  ЗАЯВЛЕНИЕ: METR прямо предупреждает: time horizon — НЕ длительность автономной работы ИИ, а объём заменяемого последовательного человеческого труда при 50% успеха; reliability-critical и плохо верифицируемые задачи требуют 98%+ успеха (такие горизонты вообще не фитятся без сьютов на порядки больше); по доменам разброс до 40-100x (визуальный computer use).
  ЗРЕЛОСТЬ: 2026-01-22, свежая методологическая заметка самих авторов метрики
  ПРИМЕНЕНИЕ: Прямое следствие для контура: длину смены задаёт не 50%-горизонт, а верифицируемость шага. Задачи с автоматической проверкой (тесты, build) можно давать кусками ближе к 80%-горизонту; плохо верифицируемые (дизайн, миграции данных) — резать мельче и ставить внешнего оценщика на каждом майлстоуне. Гейт ТЗ у пользователя уже стоит в правильном месте — там, где цена ошибки максимальна.
  URL: https://metr.org/notes/2026-01-22-time-horizon-limitations/
  ПОПРАВКА: Заявление точное. Две микро-детали для полноты: (а) фраза про 98%+ в источнике заканчивается «...to be worth automating» (речь о пороге, при котором автоматизация оправдана); (б) про 99%+ горизонты источник говорит «much larger and higher-quality benchmarks» (~300 разнообразных задач на временной бакет, шум разметки <1%), а не буквально «на порядки больше» — пересказ кандидата чуть сильнее буквальной формулировки, но смысл сохранён.

[A5-autonomy-horizon|paper] The Illusion of Diminishing Returns: Measuring Long Horizon Execution in LLMs (arXiv 2509.09677)
  ЗАЯВЛЕНИЕ: Механизм обвала на длинных прогонах — self-conditioning: увидев свои прошлые ошибки в контексте, модель ошибается чаще (эффект не лечится скейлингом размера модели, но смягчается thinking). Thinking-модели исполняют на порядки длиннее за один ход: GPT-5 (thinking) >2100 шагов, Claude-4 Sonnet (thinking) 432 шага, DeepSeek-V3 без thinking — меньше 4. Малый прирост пошаговой точности компаундится в экспоненциальный рост горизонта (H = ln(s)/ln(p), резкий рост после ~80% пошаговой точности).
  ЗРЕЛОСТЬ: Сентябрь 2025, v3 актуальна; активно цитируется в 2026-работах по long-horizon
  ПРИМЕНЕНИЕ: Прямой дизайн-принцип конвейера: майлстоун = граница контекста. После каждого майлстоуна (и особенно после любого фейла) — kill инстанса и рестарт с чистым контекстом от STATE.md + git, а не «дочинивание» в сессии, где уже лежат собственные ошибки. Плюс: включать extended thinking для build-фазы — это документированно удлиняет исполнение за один ход.
  URL: https://arxiv.org/abs/2509.09677
  ПОПРАВКА: Коррекция не требуется. Информационный нюанс: цифры 2100 / 432 / «even four steps» — из актуальной (последней, v3) редакции статьи; в первой версии (v1) стояло «over 1000 steps» для GPT-5 и «even two steps» для DeepSeek-V3. Кандидат корректно отражает текущую версию. Также в тексте статьи рост горизонта от пошаговой точности формально назван гиперболическим (hyperbolically); «экспоненциальный рост» — формулировка абстракта про компаундинг прироста точности в длину задач.

[A5-autonomy-horizon|paper] Beyond pass@1: A Reliability Science Framework for Long-Horizon LLM Agents (arXiv 2603.29231)
  ЗАЯВЛЕНИЕ: Систематический замер деградации с горизонтом (23 392 эпизода, 10 open-source моделей): aggregate pass@1 падает с 76.3% на коротком горизонте до 52.1% на очень длинном (−24.3 п.п.); в software engineering — двукратный коллапс (GDS 0.90 → 0.44). Декомпозиция задач названа интервенцией с максимальным рычагом по надёжности; memory-скаффолды длинный горизонт НЕ улучшают против простого ReAct.
  ЗРЕЛОСТЬ: Март 2026, свежайшая; ограничение — только open-source модели (DeepSeek V3, Llama 3.3 70B, Qwen3 32B и др.), переносимость абсолютных чисел на Claude ограничена, но направление эффектов совпадает с METR
  ПРИМЕНЕНИЕ: Прямая валидация архитектуры пользователя: фазовый конвейер с майлстоунами (= декомпозиция) — главный рычаг надёжности длинного прогона, а наращивание «памяти» агента — нет. Для SE-задач деградация самая крутая → именно кодовые задачи резать агрессивнее всего; ставку делать на STATE.md+git (внешняя декомпозиция), а не на длинные сессии с большим контекстом.
  URL: https://arxiv.org/html/2603.29231v1
  ПОПРАВКА: Коррекция по существу не требуется. Два мелких уточнения: (1) кривая pass@1 немонотонна — 76.3% (short) → 59.8% (medium) → 50.5% (long) → 52.1% (very-long), т.е. минимум на "long", а не на "very-long"; заявление цитирует авторскую формулировку и не искажает её. (2) В дословном тексте абстракта — "10 models" без слова "open-source"; характеристика "open-source" взята из текста статьи по пересказу (согласуется со стоимостью всего прогона ~$40.83), но в абстракте дословно не фигурирует.

[A5-autonomy-horizon|paper] Vending-Bench: долгосрочная когерентность автономных агентов (Andon Labs, arXiv 2502.15840)
  ЗАЯВЛЕНИЕ: На прогонах >20M токенов даже лучшие модели имеют прогоны, которые «сходят с рельсов»: meltdown-петли, забытые заказы, из которых агент редко выбирается сам; дисперсия между прогонами одной модели высокая; провалы НЕ коррелируют с заполнением контекстного окна — то есть это не проблема памяти, и компакция сама по себе не спасёт.
  ЗРЕЛОСТЬ: Февраль 2025; бенчмарк живой и продолжает использоваться лабораториями в 2025-2026, но числа по конкретным моделям — начала 2025
  ПРИМЕНЕНИЕ: Требование к оркестратору: watchdog на meltdown (детект зацикливания/отсутствия прогресса — например, N итераций без нового коммита или диффа) с принудительным kill + рестартом от последнего STATE.md. Высокая дисперсия между прогонами означает: ретрай с чистого чекпойнта — штатный механизм очереди, а не исключение; «спасать» деградировавший прогон дороже, чем перезапустить.
  URL: https://arxiv.org/abs/2502.15840
  ПОПРАВКА: Заявление подтверждается; коррекции не требуется. Два минорных уточнения: (1) вывод «компакция сама по себе не спасёт» — инференс ресерчера, дословно в статье его нет (статья говорит лишь «breakdowns do not stem from memory limits»), но он не искажает смысл; (2) на arXiv-листинге аффилиация авторов не указана — принадлежность к Andon Labs подтверждается внешне (авторы — основатели Andon Labs), а не самой страницей arXiv.

[A6-hitl-gates|paper] HULA — Human-In-the-Loop Software Development Agents (Atlassian, arXiv 2411.12924)
  ЗАЯВЛЕНИЕ: Единственный опубликованный продакшн-деплой конвейера с обязательным гейтом утверждения плана человеком ДО кодогенерации: в Atlassian JIRA практики утвердили 82% сгенерированных планов (433/527); после гейта 25% issues дошли до PR, из них 59% PR смержены. Числа показывают: гейт плана дёшев (большинство — быстрое «да»), а основной отсев происходит ПОСЛЕ утверждения плана — на этапе кода.
  ЗРЕЛОСТЬ: arXiv v2 (янв 2025); фреймворк задеплоен в Atlassian JIRA для внутреннего использования (заявлено в статье); есть follow-up 2506.11009 (июнь 2025) — тема живая
  ПРИМЕНЕНИЕ: Прямая валидация архитектуры пользователя: гейт ТЗ после research — правильное место (до дорогой сборки). 82% approval означает, что утренний батч из ~10 гейтов реален по времени (большинство — быстрые подтверждения), и гейт не станет бутылочным горлышком очереди; закладывать в конвейер метрику «доля возвратов с гейта» как у HULA.
  URL: https://arxiv.org/abs/2411.12924
  ПОПРАВКА: Числа и контекст точны. Две правки: (1) «Единственный опубликованный продакшн-деплой» — уникальность статьёй не заявляется и из первоисточника непроверяема; смягчить до «один из первых/редких опубликованных продакшн-деплоев». (2) 25% raised PR rate считается от 376 code-generated issues, а не от 433 утверждённых планов (от них — 22%): между утверждением плана и генерацией кода тоже был отсев (433→376). Вывод «основной отсев после гейта плана» остаётся верным.

[A6-hitl-gates|paper] InferAct — превентивная оценка действий агента + эскалация человеку (arXiv 2407.11843, EMNLP 2025)
  ЗАЯВЛЕНИЕ: Схема «оценщик проверяет траекторию агента ДО исполнения критического/необратимого действия и будит человека только при риске» детектирует рассогласованные действия до +20% Macro-F1 лучше базлайнов (пост-хок рефлексии) на 3 задачах (web shopping, household, QA) с разными LLM. Наука про размещение гейта: до необратимого шага, а не после.
  ЗРЕЛОСТЬ: Принята на EMNLP 2025 (указано на arXiv abs); v4 от 30 сен 2025 — свежая рецензированная работа
  ПРИМЕНЕНИЕ: Паттерн для фазы ПОСЛЕ гейта ТЗ (полная автономия): не полный bypass, а PreToolUse-хук/оценщик, который пропускает рутину и эскалирует (паркует задачу + уведомление) только необратимые действия — деплой, миграции, force push. Селективная эскалация вместо «спрашивать всё» или «не спрашивать ничего».
  URL: https://arxiv.org/abs/2407.11843
  ПОПРАВКА: Две поправки к формулировке кандидата: (1) «+20% Macro-F1 лучше базлайнов (пост-хок рефлексии)» — неверно про базлайны: +20% («up to», среднее ~8% над сильнейшим) достигается против превентивных оценочных базлайнов (Direct Prompt, Self-Consistency, Token Probability, Token Entropy, Multi-step Evaluation), а не против пост-хок рефлексии; сравнение с self-reflection есть только в Appendix B. Противопоставление «до, а не после» — концептуальная рамка статьи, но число измерено не против пост-хок методов. (2) Вторая «цитата» метода — парафраз, не дословная: «unsafe actions» и «alerting humans for intervention» отсутствуют в абстракте (и v1, и v4); дословно есть «preemptive evaluation» (v1) и «detect misaligned actions before execution… alerts users for timely correction» (v4). Также актуальное название статьи — «Preemptive Detection and Correction of Misaligned Actions in LLM Agents» (InferAct — имя метода; старое название с «InferAct» было в v1). Метрика в тексте статьи — Macro-F1; «Marco-F1» — опечатка самих авторов в абстракте.

[A6-hitl-gates|official-docs] Claude Code sandboxing: −84% permission-промптов (Anthropic engineering + code.claude.com/docs/en/sandboxing)
  ЗАЯВЛЕНИЕ: OS-уровневый sandbox (файловая + сетевая изоляция на Linux bubblewrap / macOS Seatbelt — нативно для Mac пользователя) по внутренним данным Anthropic безопасно убирает 84% permission-промптов; рантайм открыт (anthropic-experimental/sandbox-runtime). Это официальная компенсация безнадзорного режима: чем меньше промптов, тем меньше парковок задач в очереди.
  ЗРЕЛОСТЬ: Пост от 20 окт 2025; sandbox-runtime — open source research preview; фича в актуальных доках code.claude.com/docs/en/sandboxing (жива, развивается)
  ПРИМЕНЕНИЕ: Каждый headless-инстанс очереди гонять в sandbox с whitelist сети: 84% действий перестают требовать человека → меньше ложных парковок. Дока permission-modes прямо говорит: bypassPermissions «Only use this mode in isolated environments like containers, VMs…», причём «The check is skipped automatically inside a recognized sandbox» — sandbox официально легализует автономный режим на локальном Mac.
  URL: https://www.anthropic.com/engineering/claude-code-sandboxing
  ПОПРАВКА: Заявление точное. Уточнения-нюансы (не ошибки): 84% — внутренняя метрика Anthropic (self-reported, без внешней валидации), что кандидат корректно атрибутирует; в блоге фича представлена как beta/research preview, но по актуальной доке (code.claude.com/docs/en/sandboxing) sandbox встроен в Claude Code (macOS Seatbelt без установки, Linux/WSL2 требует bubblewrap+socat); дока явно предупреждает, что sandbox — не полный isolation boundary (TLS по умолчанию не инспектируется, возможен domain fronting при широких allowedDomains). Репозиторий anthropic-experimental/sandbox-runtime жив: последний push 2026-07-14, 4670 звезд, не archived.

[A6-hitl-gates|official-docs] Claude Code auto mode: классификатор вместо bypass; approval fatigue = 93% (Anthropic engineering, март 2026)
  ЗАЯВЛЕНИЕ: Пользователи одобряют 93% permission-промптов (ручные синхронные гейты выродились в кликанье «да») → Anthropic заменил их model-based классификатором: 0.4% ложных блокировок на реальном трафике, 5.7% пропусков на синтетической эксфильтрации. Официальная середина между ручным ревью и --dangerously-skip-permissions. Критично для очереди: в headless (-p) повторные блокировки классификатора абортят сессию — «If the classifier blocks an action 3 times in a row or 20 times total» (permission-modes doc), т.к. промптовать некого.
  ЗРЕЛОСТЬ: Пост 25 марта 2026 — свежайший; режим в актуальных доках (--permission-mode auto), доступен на всех планах включая Max
  ПРИМЕНЕНИЕ: Для фазы после гейта ТЗ: --permission-mode auto вместо --dangerously-skip-permissions — автономия с фоновыми проверками. Явные permissions.ask-правила работают и в auto (принудительный гейт на деплой/push). Оркестратор ОБЯЗАН обрабатывать аборт сессии по повторным блокировкам: ловить exit, фиксировать парковку в STATE.md, уведомлять, брать следующую задачу — это штатное, документированное поведение, а не сбой.
  URL: https://www.anthropic.com/engineering/claude-code-auto-mode
  ПОПРАВКА: Заявление точное. Единственное уточнение (кандидат его уже учёл): пороги «3 подряд / 20 всего» в интерактивном режиме дают паузу auto mode и возврат к ручным промптам, а не аборт; аборт сессии — только в non-interactive (-p). Пороги не конфигурируемы.

[A6-hitl-gates|official-docs] Механика парковки в headless: --permission-prompt-tool + хуки Notification/PreToolUse/PermissionRequest (code.claude.com)
  ЗАЯВЛЕНИЕ: Claude Code имеет штатную точку врезки для «парковка на гейте + уведомление + переключение на следующую задачу»: флаг --permission-prompt-tool делегирует permission-промпты неинтерактивного режима вашему MCP-инструменту (который может записать вопрос в очередь, дёрнуть push-уведомление и вернуть deny = припарковать); хук Notification с матчерами permission_prompt / agent_needs_input / agent_completed — триггер уведомлений; PreToolUse возвращает permissionDecision allow/deny/ask — программируемый селективный гейт.
  ЗРЕЛОСТЬ: Актуальные официальные доки (открыты 2026-07-15, упоминают v2.1.199–2.1.208) — активно поддерживаются
  ПРИМЕНЕНИЕ: Ядро контура: оркестратор запускает claude -p --permission-prompt-tool <mcp-очередь>; на гейте ТЗ инстанс задаёт вопрос → MCP-тул пишет его в трекер, шлёт уведомление на телефон, возвращает deny → инстанс корректно завершается, STATE.md хранит точку резюма → оркестратор берёт следующий проект. Утром/вечером пользователь батчем отвечает, оркестратор резюмит по STATE.md. Ровно требуемая семантика «park + notify + switch».
  URL: https://code.claude.com/docs/en/cli-reference
  ПОПРАВКА: Суть верна, но 4 уточнения: (1) цитата «permission_prompt — Permission dialog appears» неточна: "When a permission dialog appears" — это описание СОБЫТИЯ PermissionRequest; реальное описание матчера permission_prompt — "Claude needs you to approve a tool use". (2) Матчеры agent_needs_input/agent_completed срабатывают ТОЛЬКО при открытом agent view (v2.1.198+) — для чистого headless-сценария не годятся; для headless-парковки рабочие точки — --permission-prompt-tool, PreToolUse и Notification(permission_prompt). (3) PermissionRequest-хуки не срабатывают в non-interactive режиме (-p): доки советуют PreToolUse для автоматических permission-решений в headless. (4) С v2.1.199 permission-prompt-tool не может approve MCP-инструмент, помеченный "requiring user interaction" — allow конвертируется в deny; также Claude Code ждёт подключения MCP-сервера этого инструмента до MCP_TIMEOUT (30 с).

[A6-hitl-gates|official-docs] GitHub Copilot coding agent: парковка на PR + гейт «Approve and run workflows» (docs.github.com)
  ЗАЯВЛЕНИЕ: Крупнейший продакшн-агент паркует автономную работу на артефакте (pull request): по умолчанию GitHub Actions НЕ запускаются на пушах Copilot, пока человек не нажмёт «Approve and run workflows» — гейт стоит перед привилегированной/дорогой работой (CI с секретами), а итерация асинхронна (комментарий @copilot). С марта 2026 гейт можно отключить настройкой — т.е. он осознанный default, а не техограничение.
  ЗРЕЛОСТЬ: Актуальная официальная дока GA-продукта; changelog 13 марта 2026 добавил опцию skip approval — активно развивается
  ПРИМЕНЕНИЕ: Готовый паттерн «артефакт = парковка»: агент доводит работу до проверяемого артефакта (у пользователя — ТЗ в docs/, коммит в ветке) и останавливается перед следующей дорогой фазой; человек утверждает асинхронно, батчем, через интерфейс, а не синхронный диалог. Подтверждает дизайн: гейт живёт на артефакте в git, не в живой сессии.
  URL: https://docs.github.com/en/copilot/how-tos/use-copilot-agents/coding-agent/review-copilot-prs
  ПОПРАВКА: Существенных исправлений нет. Уточнения: дата отключаемости гейта — точно 13 марта 2026 (GitHub Changelog «Optionally skip approval for Copilot coding agent Actions workflows», настройка на уровне репозитория, доступна админам); в актуальной доке продукт местами переименован в «Copilot cloud agent» (дока: «Optionally, you can configure Copilot cloud agent to allow GitHub Actions workflows to run without human intervention»). «Крупнейший продакшн-агент» — оценка ресерчера, источником не утверждается.

[A6-hitl-gates|official-docs] Devin Interactive Planning: гейт плана с тайм-аутом и опцией жёсткого ожидания (docs.devin.ai)
  ЗАЯВЛЕНИЕ: Devin ставит гейт утверждения плана ДО автономного исполнения, но по умолчанию мягкий: ждёт отзыв 30 секунд и продолжает сам; кнопка «Wait for my approval» превращает гейт в жёсткий (ждать сколько угодно); план содержит code citations для инспекции до одобрения. Демонстрирует спектр «мягкий гейт с тайм-аутом ↔ жёсткий гейт» как настройку, а не архитектуру.
  ЗРЕЛОСТЬ: Актуальная официальная дока Cognition (release notes ведутся по 2026) — живой коммерческий продукт
  ПРИМЕНЕНИЕ: Для контура пользователя гейт ТЗ — жёсткий (аналог «Wait for my approval», auto-proceed запрещён). Но Devin подсказывает полезную вторую ручку: для НЕкритичных вопросов после гейта — «спросить и продолжить по дефолту через тайм-аут», чтобы припаркованная мелочь не блокировала ночной прогон. В ТЗ гейта стоит явно разделить блокирующие и тайм-аут-вопросы.
  URL: https://docs.devin.ai/work-with-devin/interactive-planning
  ПОПРАВКА: Заявление точно. Единственный микронюанс: дока не говорит буквально «ждать сколько угодно» для «Wait for my approval» — формулировка «to ensure Devin waits for your feedback on its complete plan» (без явного тайм-аута), что по смыслу соответствует жёсткому ожиданию в противовес 30-секундному дефолту.

[A6-hitl-gates|paper] OASIS / отложенная доставка уведомлений к breakpoints (Iqbal & Bailey, ACM TOCHI)
  ЗАЯВЛЕНИЕ: Доставка уведомлений на границах задач (breakpoints) вместо немедленной снижает фрустрацию на 20% и время реакции на 25%, при отсрочке в среднем всего ~90 секунд; более крупные границы дают ещё меньшую стоимость прерывания. Научное обоснование асинхронных батч-утверждений (утро/вечер) vs синхронных прерываний: чем крупнее граница, к которой отложено уведомление, тем дешевле оно для человека.
  ЗРЕЛОСТЬ: ACM TOCHI (рецензируемый журнал, ~2010); классика interruption science — результаты старые, но устойчивые и до сих пор цитируются в работах об уведомлениях
  ПРИМЕНЕНИЕ: Обоснование ключевого UX-решения контура: уведомления о гейтах НЕ дёргают пользователя синхронно, а копятся в очереди и доставляются к «границам» его дня (утренний батч постановки + вечерний батч утверждений) — прямой перенос breakpoint-принципа. Вместе с фактом «users approve 93% of prompts» (Anthropic) даёт полную картину: синхронные гейты и дороги, и бесполезны из-за fatigue.
  URL: https://www.microsoft.com/en-us/research/wp-content/uploads/2016/02/TOCHI-Oasis-final.pdf
  ПОПРАВКА: Заявление точное, две оговорки: (1) числа 20%/25%/~90 сек получены в полевом исследовании ТОЛЬКО для политики defer-to-next-breakpoint vs немедленная доставка (остальные политики количественно не тестировались); (2) тезис «крупнее граница → ниже стоимость» в статье опирается на их прежние контролируемые исследования (Iqbal & Bailey 2006), а перенос на многочасовые батчи (утро/вечер) — экстраполяция за пределы измеренного (отсрочки в статье ~90 сек): направление поддержано, масштаб не проверялся.

[A7-reliability|official-docs] Claude Code OpenTelemetry: метрики и события (monitoring-usage)
  ЗАЯВЛЕНИЕ: Claude Code штатно экспортирует OTel-метрики (claude_code.token.usage, cost.usage, session.count, active_time.total, lines_of_code.count, commit.count) и события (api_request, api_error, tool_result, tool_decision, compaction, internal_error и др.) через otlp/prometheus/console; включается CLAUDE_CODE_ENABLE_TELEMETRY=1 + OTEL_METRICS_EXPORTER/OTEL_LOGS_EXPORTER; каждая метрика и событие несут session.id (включено по умолчанию), интервал экспорта логов по умолчанию 5 с; есть beta распределённого трейсинга (CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1) и гейты детализации OTEL_LOG_TOOL_DETAILS/OTEL_LOG_USER_PROMPTS.
  ЗРЕЛОСТЬ: Официальная дока, актуальна на июль 2026 (упоминает v2.1.193+; distributed tracing в бете)
  ПРИМЕНЕНИЕ: Ядро наблюдаемости демона: оркестратор ставит env при спавне инстансов, локальный OTel Collector собирает per-session метрики по session.id (session.id из claude -p --output-format json связывается с задачей очереди). Watchdog-правила: нет claude_code.api_request N минут при статусе working = зависание; поток tool_result/tool_decision с OTEL_LOG_TOOL_DETAILS=1 = сырьё для внешнего детекта зацикливания (повтор идентичных tool-вызовов). Готовые дашборды поверх этого: ColeMurray/claude-code-otel (Collector+Prometheus+Loki+Grafana, 466 звёзд, активен, панели Performance & Errors и Event Logs) и rockdarko/claude-code-metrics-prometheus (Grafana dashboard #25255, свежий, PromQL по claude_code_* метрикам).
  URL: https://code.claude.com/docs/en/monitoring-usage
  ПОПРАВКА: Заявление точное. Единственный микронюанс: prometheus — вариант только для OTEL_METRICS_EXPORTER (otlp/prometheus/console/none); для OTEL_LOGS_EXPORTER варианты otlp/console/none, без prometheus. Также список событий в доке шире заявленного (api_refusal, assistant_response, auth, mcp_server_connection, hook_*, plugin_*, skill_activated и др.), что покрыто оговоркой «и др.».

[A7-reliability|official-docs] Headless-контракт claude -p: api_retry, exit-коды, потолки зависаний
  ЗАЯВЛЕНИЕ: В stream-json есть системное событие api_retry с категорией ошибки (rate_limit, overloaded, billing_error, authentication_failed, server_error…), attempt/max_retries и retry_delay_ms — ретраи видимы снаружи; --output-format json отдаёт total_cost_usd и session_id для каждого прогона; переполнение stdin >10MB даёт non-zero exit; зависший фоновый сабагент не держит claude -p дольше 10 минут (настраивается CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS); --max-turns из cli-reference — жёсткая внешняя граница цикла: выходит с ошибкой при достижении лимита; есть и --max-budget-usd.
  ЗРЕЛОСТЬ: Официальная дока, актуальна на июль 2026 (поведения привязаны к v2.1.16x–2.1.2xx)
  ПРИМЕНЕНИЕ: Контракт для воркера конвейера: каждый прогон фазы = claude -p --output-format stream-json; оркестратор парсит api_retry (rate_limit/overloaded = пауза очереди, не убийство задачи; billing/auth = стоп + уведомление), по result-сообщению и exit-коду решает ретрай. --max-turns на каждую фазу = страховка от бесконечного цикла, идемпотентный ретрай затем через --resume session_id с STATE.md. Важно: --bare рекомендован для скриптов, но «Bare mode skips OAuth and keychain reads. Anthropic authentication must come from ANTHROPIC_API_KEY or an apiKeyHelper» — для Max-подписки запускать БЕЗ --bare.
  URL: https://code.claude.com/docs/en/headless
  ПОПРАВКА: Заявление точно. Мелкие уточнения для полноты: потолки версионно-гейтированы — лимит stdin 10MB с v2.1.128, 10-минутный потолок ожидания фонового сабагента с v2.1.182 (CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS=0 отключает лимит); полный список категорий error в api_retry шире перечисленного: ещё invalid_request, model_not_found, max_output_tokens, unknown; --max-turns и --max-budget-usd задокументированы на странице cli-reference, а не на самой странице headless.

[A7-reliability|official-docs] Agent view + встроенный супервизор (claude agents / claude --bg / claude daemon)
  ЗАЯВЛЕНИЕ: В Claude Code уже есть официальный локальный демон фоновых сессий: per-user супервизор с состояниями (working/blocked/done/failed/stopped), claude agents --json для скриптового поллинга (включая waitingFor: «permission prompt»/«input needed»), ~/.claude/daemon.log и daemon/roster.json для реконнекта после рестарта, автоперезапуск зависшей сессии с продолжением прерванного ответа, нотификации через Notification-hook (agent_needs_input/agent_completed), автоматическая изоляция каждой фоновой сессии в git worktree под .claude/worktrees/, управление claude daemon status/stop.
  ЗРЕЛОСТЬ: Research preview (официально в доке), поведение зафиксировано для v2.1.19x; активно развивается на июль 2026
  ПРИМЕНЕНИЕ: Самый сильный кандидат: половина демона уже написана Anthropic. Утренняя очередь = серия claude --bg --name task-N; парковка на гейте ТЗ = состояние blocked + waitingFor: input needed + Notification-hook шлёт пуш пользователю; оркестратору остаётся тонкий слой: поллить claude agents --json, соблюдать сериализацию задач одного проекта и отвечать на гейте через claude attach / peek-reply. Восстановление: супервизор сам рестартует зависшие процессы и переживает автообновление бинаря (сессии — detached-процессы). Инвариант «один пишущий на репо» частично закрыт авто-worktree; осторожно: удаление сессии сносит её worktree вместе с незакоммиченным.
  URL: https://code.claude.com/docs/en/agent-view
  ПОПРАВКА: Заявление в целом точно. Два уточнения: (1) «автоперезапуск зависшей сессии» срабатывает при ОТКРЫТИИ переставшей отвечать сессии (on-open), а не автоматически в фоне, и это фича v2.1.200+, а не v2.1.198; (2) изоляция в git worktree происходит «before editing files» (лениво, перед первой записью), а не безусловно для каждой фоновой сессии при старте. Также команда остановки в доке — «claude daemon stop --any» (с флагом).

